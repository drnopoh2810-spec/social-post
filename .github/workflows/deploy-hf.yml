name: Deploy to HuggingFace Spaces

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    name: Push to HuggingFace Space
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to HuggingFace Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_SPACE: ${{ secrets.HF_SPACE }}
        run: |
          if [ -z "${HF_TOKEN}" ] || [ -z "${HF_SPACE}" ]; then
            echo "⚠️ HF_TOKEN or HF_SPACE not set — skipping"
            exit 0
          fi

          git config --global user.email "ci@github.com"
          git config --global user.name "GitHub Actions"

          # Clone HF Space into a temp folder OUTSIDE the repo
          git clone https://user:${HF_TOKEN}@huggingface.co/spaces/${HF_SPACE} /tmp/hf_space

          # Sync social_post/ subfolder → HF Space root
          # Source ends with / = copy contents, not the folder itself
          rsync -av --delete \
            --exclude='.git' \
            --exclude='.env' \
            --exclude='*.db' \
            --exclude='*.sqlite' \
            --exclude='__pycache__' \
            --exclude='*.pyc' \
            social_post/ /tmp/hf_space/

          cd /tmp/hf_space
          git add -A
          git diff --staged --quiet && echo "Nothing to deploy" && exit 0
          git commit -m "Deploy: $(git -C $GITHUB_WORKSPACE rev-parse --short HEAD)"
          git push
          echo "✅ Deployed to: ${HF_SPACE}"
